const SEP = '/';

//Domain
const APP = 'app';
const MAIN = 'main';

//Action
const BOOT = 'boot';
const GET_USERS = 'getUsers';

// Action Types
export const APP_BOOT = APP + SEP + BOOT;
export const MAIN_GET_USERS = MAIN + SEP + GET_USERS;
